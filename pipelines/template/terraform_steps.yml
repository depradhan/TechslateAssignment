parameters:
  - name: ringName
    type: string

steps:
  - task: TerraformInstaller@1
    displayName: 'Install Terraform Tool'
    inputs:
      terraformVersion: 'latest'

  - task: TerraformTaskV4@4
    displayName: 'Terraform init'
    inputs:
      provider: 'azurerm'
      command: 'init'
      commandOptions: '-backend-config="./backend/backend-${{parameters.ringName}}.tfvars"'
      backendServiceArm: 'InsightApp'
      workingDirectory: '$(System.DefaultWorkingDirectory)/terraform'


  - task: TerraformTaskV4@4
    displayName: 'Terraform Validate'
    inputs:
      provider: 'azurerm'
      command: 'validate'
      workingDirectory: '$(System.DefaultWorkingDirectory)/terraform'

  - task: TerraformTaskV4@4
    displayName: 'Terraform Plan'
    inputs:
      provider: 'azurerm'
      command: 'plan'
      commandOptions: '-var-file="../../terraform/variables/${{parameters.ringName}}.tfvars"'
      environmentServiceNameAzureRM: 'InsightApp'
      workingDirectory: '$(System.DefaultWorkingDirectory)/terraform'

  - task: TerraformTaskV4@4
    displayName: 'Terraform Apply'
    inputs:
      provider: 'azurerm'
      command: 'plan'
      commandOptions: '-var-file="../../terraform/variables/${{parameters.ringName}}.tfvars" -auto-approve'
      environmentServiceNameAzureRM: 'InsightApp'
      workingDirectory: '$(System.DefaultWorkingDirectory)/terraform'
